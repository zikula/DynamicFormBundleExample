{% extends 'base.html.twig' %}

{% block stylesheets %}
    <style>
    table, th, td {
        border: 1px solid;
        border-collapse: collapse;
        padding: 4px;
    }
    </style>
{% endblock%}

{% block title %}Survey{% endblock %}

{% block body %}
    <h1>Survey #{{ survey.id }}</h1>
    <a href="{{ path('app_survey_index') }}">back to list</a>

    <h2>Survey responses</h2>
    <table>
        <thead>
            <th>Email</th>{# you are responsible for your own non-dynamic fields #}
            {% for key, label in survey.labels %}
                <th>{{ label }}</th>
            {% endfor %}
            <th>View</th>
        </thead>
        <tbody>
        {% for response in survey.responses %}
            <tr>
                <td>{{ response.email }}</td>{# you are responsible for your own non-dynamic fields #}
                {% for key, label in survey.labels %}
                    <td>{{ attribute(response, key)|default }}</td>
                {% endfor %}
                <td><a href="{{ path('app_survey_response_view', {id: response.id})}}">view</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
