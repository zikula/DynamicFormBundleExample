{% extends 'base.html.twig' %}

{% block title %}Edit Survey{% endblock %}

{% block body %}
    <h1>Edit Survey</h1>

    {{ include('survey/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_survey_index') }}">back to list</a>

    {{ include('survey/_delete_form.html.twig') }}

    <div style="margin-top: 10em;">
        <span><a href="?XDEBUG_SESSION_START=phpstorm"><i class="fa-solid fa-bug"></i> XDebug session start</a></span>
    </div>
    <div>
        <span><a href="?XDEBUG_SESSION_STOP"><i class="fa-solid fa-bug-slash"></i> XDebug session stop</a></span>
    </div>

{% endblock %}
