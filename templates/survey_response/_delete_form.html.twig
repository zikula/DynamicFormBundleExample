<form method="post" action="{{ path('app_survey_response_delete', {'id': survey_response.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ survey_response.id) }}">
    <button class="btn">Delete</button>
</form>
